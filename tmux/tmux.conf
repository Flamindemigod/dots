# reload config file (change file location to your the tmux.conf you want to use)
bind r source-file ~/.config/tmux/tmux.conf
# Enable mouse control (clickable windows, panes, resizable panes)
set -g mouse on

# don't rename windows automatically
set-option -g allow-rename off

#Vim Keymaps to change panes
setw -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

set -g @plugin "tmux-plugins/tpm"
set -g @plugin 'rose-pine/tmux'

set -g @rose_pine_variant 'main' # Options are 'main', 'moon' or 'dawn'
# set -g @rose_pine_variant 'moon' # Options are 'main', 'moon' or 'dawn'
# set -g @rose_pine_variant 'dawn' # Options are 'main', 'moon' or 'dawn'
set -g @rose_pine_date_time "%a %d/%m/%C"
set -g @rose_pine_directory "on"

set -g @rose_pine_user "off"
set -g @rose_pine_disable_active_window_menu 'on' # Disables the menu that shows the active window on the left

# set -g @rose_pine_default_window_behavior 'off' # Forces tmux default window list behaviour
# set -g @rose_pine_show_current_program 'off' # Forces tmux to show the current running program as window name
# set -g @rose_pine_show_pane_directory 'no' # Forces tmux to show the current directory as window name
# # Previously set -g @rose_pine_window_tabs_enabled
#
# # Example values for these can be:
set -g @rose_pine_left_separator '  ' # The strings to use as separators are 1-space padded
set -g @rose_pine_right_separator '  ' # Accepts both normal chars & nerdfont icons
set -g @rose_pine_field_separator '󰇝' # Again, 1-space padding, it updates with prefix + I
set -g @rose_pine_window_separator '󰇝' # Replaces the default `:` between the window number and name
#
# # These are not padded
set -g @rose_pine_session_icon '' # Changes the default icon to the left of the session name
set -g @rose_pine_current_window_icon '' # Changes the default icon to the left of the active window name
set -g @rose_pine_folder_icon '󰉋' # Changes the default icon to the left of the current directory folder
set -g @rose_pine_username_icon '' # Changes the default icon to the right of the hostname
# set -g @rose_pine_hostname_icon '󰒋' # Changes the default icon to the right of the hostname
# set -g @rose_pine_date_time_icon '󰃰' # Changes the default icon to the right of the date module
set -g @rose_pine_window_status_separator "  " # Changes the default icon that appears between window names
#
# # Very beta and specific opt-in settings, tested on v3.2a, look at issue #10
set -g @rose_pine_prioritize_windows 'off' # Disables the right side functionality in a certain window count / terminal width
set -g @rose_pine_width_to_hide '80' # Specify a terminal width to toggle off most of the right side functionality
set -g @rose_pine_window_count '5' # Specify a number of windows, if there are more than the number, do the same as width_to_hide


set -g @rose_pine_status_left_prepend_section 'It works'

# set -g @plugin "dracula/tmux"
#
# # the default is 5, it can accept any number
# set -g @dracula-refresh-rate 5 
# set -g @dracula-border-contrast true
# set -g @dracula-show-powerline true
# #
# # available plugins: battery, cpu-usage, git, gpu-usage, ram-usage, tmux-ram-usage, network, network-bandwidth, network-ping, ssh-session, attached-clients, network-vpn, weather, time, mpc, spotify-tui, playerctl, kubernetes-context, synchronize-panes
# set -g @dracula-plugins "mpc git time"
# set -g @dracula-show-empty-plugins false
#
# # set -g @dracula-show-timezone false
# # set -g @dracula-day-month true
# set -g @dracula-time-format "%a %d/%m/%C"
# set -g @dracula-git-colors "light_purple dark_gray"
# set -g @dracula-git-disable-status false 
# # default is ✓. Avoid using non unicode characters that bash uses like $, * and !
# set -g @dracula-git-show-current-symbol \ueafc
# # default is unicode !. Avoid bash special characters
# set -g @dracula-git-show-diff-symbol \ue725
#
# # default is unicode no message
# set -g @dracula-git-no-repo-message ""
#
# # default is false
# set -g @dracula-git-no-untracked-files true
#
# set -g @dracula-mpc-colors "dark_gray green"
# set -g @dracula-mpc-max-len 45
#
# set -g @dracula-show-flags true
# set -g @dracula-show-left-icon 󰣇 
#
run "~/.config/tmux/plugins/tpm/tpm"
